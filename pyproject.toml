[project]
name = "nagare-contracts"
version = "0.1.0"
description = "Shared contracts for the Nagare trading system (jcdd/nagare/ronin/torii)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["setuptools>=75.0"]
build-backend = "setuptools.build_meta"
